{
  "name": "skvoid-platform",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/frontend",
    "apps/backend",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"npm run dev --workspace apps/frontend\" \"npm run dev --workspace apps/backend\"",
    "build": "npm run build --workspace apps/frontend && npm run build --workspace apps/backend",
    "start": "npm run start --workspace apps/backend",
    "lint": "npm run lint --workspace apps/frontend && npm run lint --workspace apps/backend",
    "test": "npm run test --workspace apps/backend && npm run test --workspace apps/frontend",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.3.3"
  }
}
